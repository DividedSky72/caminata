application: river-glen
dispatch:

  # Laps incrementer uses the laps service.
  - url: "*/laps/*"
    service: laps

  # Laps incrementer uses the laps service.
  - url: "*/barcodes/*"
    service: barcodes
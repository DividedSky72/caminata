dispatch:

  # Laps incrementer uses the laps service.
  - url: "*/laps/*"
    service: laps

  # Laps incrementer uses the laps service.
  - url: "*/barcodes*"
    service: barcodes
    
  # Laps incrementer uses the laps service.
  - url: "*/caminata_users*"
    service: caminata-users